"""
Example: Basic note completion using LLM API (stub)
"""

def complete_note_stub(note: str) -> str:
    """Stub function for note completion. Replace with actual LLM call."""
    return f"[Completed] {note}"

if __name__ == "__main__":
    example_note = "This is a sample note."
    print(complete_note_stub(example_note))
